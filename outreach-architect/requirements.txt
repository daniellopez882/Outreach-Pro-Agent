# Core Dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
# [email] pulls email-validator, which pydantic.EmailStr requires. main.py uses
# EmailStr, so without it the app fails at import -- it worked locally only
# because the package happened to be present in the dev virtualenv.
pydantic[email]>=2.7,<3.0
# NoDecode (used in config.py) needs >=2.6. The previous pin of 2.1.0 predates
# it, so the container failed at import while a fresh local venv worked.
pydantic-settings>=2.6,<3.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
redis==5.0.1

# Task Queue
celery==5.3.6
kombu==5.3.5

# HTTP & HTML parsing
# selenium and playwright are gone with the credential scraper they drove;
# see docs/data-sourcing.md.
httpx==0.26.0
requests==2.31.0
beautifulsoup4==4.12.3

# LLM Integration (Kimi 2.5 compatible)
openai==1.10.0  # Use OpenAI-compatible SDK for Kimi
anthropic==0.18.1  # Fallback option

# Data Processing
pandas==2.2.0
python-dateutil==2.8.2

# Email
sendgrid==6.11.0
jinja2==3.1.3

# Utilities
python-dotenv==1.0.0
loguru==0.7.2
tenacity==8.2.3
ratelimit==2.2.1

# Monitoring
prometheus-client==0.19.0
